export default function NotFound() {
  return (
    <main className="page-shell">
      <section className="card">
        <h1 style={{ marginTop: 0 }}>Not Found</h1>
        <p className="muted">That claim token is invalid or no longer available.</p>
      </section>
    </main>
  );
}
